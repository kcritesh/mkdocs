import React from "react";

const TransformDataCard = ({ title, images, desc, color }: Props) => {
  return <div>TransformDataCard</div>;
};

export default TransformDataCard;

interface Props {
  title: string;
  images: string[];
  desc: string;
  color: string;
}
